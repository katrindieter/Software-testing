{
  "name": "softwaretesting",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:coveralls": "jest --coverage && coveralls < coverage/lcov.info",
    "test:staged": "jest --findRelatedTests",
    "coveralls": "cross-env COVERALLS_REPO_TOKEN=$COVERALLS_REPO_TOKEN jest --coverage && cat ./coverage/lcov.info | coveralls --verbose"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/katrindieter/Software-testing.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/katrindieter/Software-testing/issues"
  },
  "homepage": "https://github.com/katrindieter/Software-testing#readme",
  "devDependencies": {
    "@babel/core": "*",
    "@babel/preset-env": "*",
    "babel-jest": "*",
    "coveralls": "^3.1.1",
    "jest": "^27.3.1",
    "cross-env": "^7.0.3"
  }
}
